# Normalize line endings to LF across platforms (matters for Linux CI).
* text=auto eol=lf

# Treat these as binary (never normalize).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.avif binary
*.ico binary
*.woff binary
*.woff2 binary
